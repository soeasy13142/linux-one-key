## ============================================
## 内核安全参数模板 - linux-one-key
## 参考: CIS Benchmarks, STIG, NIST SP 800-123
## 适用: CentOS 7+, Ubuntu 20.04+, Debian 11+, Rocky/Alma
## 此文件由 kernel.sh 生成到 /etc/sysctl.d/99-hardening.conf
## ============================================

## ===== 网络安全 =====

# SYN Flood 防护：启用 SYN Cookies，防止 SYN 洪泛攻击
net.ipv4.tcp_syncookies = 1

# 反向路径过滤：验证入站数据包的源地址是否可达，防止 IP 欺骗
net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.default.rp_filter = 1

# 禁止接受 ICMP 重定向：防止中间人攻击篡改路由
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0
net.ipv6.conf.all.accept_redirects = 0
net.ipv6.conf.default.accept_redirects = 0

# 禁止发送 ICMP 重定向：本机不作为路由器时不应发送重定向
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.default.send_redirects = 0

# 禁止源路由：防止攻击者指定数据包路径
net.ipv4.conf.all.accept_source_route = 0
net.ipv4.conf.default.accept_source_route = 0
net.ipv6.conf.all.accept_source_route = 0
net.ipv6.conf.default.accept_source_route = 0

# 记录可疑数据包（Martian packets）：源地址不可能到达的包
net.ipv4.conf.all.log_martians = 1
net.ipv4.conf.default.log_martians = 1

# 忽略广播 ICMP 请求：防止 Smurf 攻击
net.ipv4.icmp_echo_ignore_broadcasts = 1

# 禁止 IP 转发：云服务器通常不需要充当路由器
net.ipv4.ip_forward = 0

# 忽略虚假 ICMP 错误响应
net.ipv4.icmp_ignore_bogus_error_responses = 1

## ===== 内核安全 =====

# ASLR 地址空间布局随机化：2=完全随机化，防止内存地址猜测攻击
kernel.randomize_va_space = 2

# 禁止 SUID 程序产生核心转储：防止敏感内存数据泄露
fs.suid_dumpable = 0

# 限制 dmesg 访问：普通用户不能读取内核日志
kernel.dmesg_restrict = 1

# 限制 kernel 指针泄露：隐藏内核指针地址
kernel.kptr_restrict = 2

# 禁止非特权 BPF：未授权用户不能加载 BPF 程序（CIS 建议）
kernel.unprivileged_bpf_disabled = 1

# 禁止 kexec 加载新内核：防止未授权内核替换（CIS 建议）
kernel.kexec_load_disabled = 1

# 限制 perf 事件暴露：防止侧信道攻击（CIS Level 2 / STIG 建议）
kernel.perf_event_paranoid = 3

## ===== ARP 安全 =====

# 限制仅回答目标 IP 为本机地址的 ARP 请求（防止 ARP 欺骗）
net.ipv4.conf.all.arp_ignore = 1
net.ipv4.conf.default.arp_ignore = 1

# 限制仅通过本机路由表中的最佳路径接口发送 ARP 通告
net.ipv4.conf.all.arp_announce = 2
net.ipv4.conf.default.arp_announce = 2
